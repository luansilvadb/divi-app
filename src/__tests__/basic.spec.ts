import { describe, it, expect } from 'vitest'

describe('Basic Test', () => {
  it('should work', () => {
    expect(true).toBe(true)
  })

  it('should add numbers', () => {
    expect(2 + 2).toBe(4)
  })
})
