## 2024-03-31 - [Icon-only Buttons Accessibility] **Aprendizado:** Many icon-only buttons in the application lack `aria-label` attributes, which makes them inaccessible to screen readers. For example, the close button in `TransactionForm.vue` modal is an icon-only button without an `aria-label`. **Ação:** Always ensure icon-only buttons and interactive elements without visible text have explicit `aria-label` or `aria-labelledby` attributes.
